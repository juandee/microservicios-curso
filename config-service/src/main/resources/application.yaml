server:
  port: 8081
spring:
  cloud:
    config:
      server:
         git:
           default-label: zipkin-leuth
           uri: https://github.com/juandee/microservicios-curso
           search-paths: config-data
  application:
    name: config-service 